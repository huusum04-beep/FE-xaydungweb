import { createApp } from 'vue'
import './style.css' // (Nếu bạn đã xóa file này thì bỏ dòng này đi nhé)
import App from './App.vue'

// Dòng lệnh quan trọng nhất để render Vue ra màn hình
createApp(App).mount('#app')